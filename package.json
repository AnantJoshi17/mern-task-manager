{
  "name": "mern-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Task manager built with MongoDB, Express, React and Node",
  "scripts": {
    "// dev": "Starts the backend and frontend together in one terminal",
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "// install-all": "Installs packages in both folders",
    "install-all": "npm install --prefix server && npm install --prefix client",
    "// build": "Builds the frontend for production",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
